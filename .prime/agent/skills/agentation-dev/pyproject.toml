[project]
name = "agentation-dev"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["httpx"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agentation_dev"]
