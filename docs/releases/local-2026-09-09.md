# Verify the local Ernie candidate

This record covers the manual Apple Silicon installation on September 9, 2026. It is a local candidate, not a published release.

## Candidate and installation

- Version: 0.2.1, normal Ernie branding
- Base source: `25458ea37b39c7b18d1313481b6c7771e676bb52`, fetched from `origin/main`
- Candidate includes the uncommitted local fixes recorded in `dist/local-candidate.patch`
- Archive: `dist/Ernie-0.2.1-arm64-mac.zip`
- Archive SHA-256: `a9f5857ecd1d7a4bd2e5651c7007a950dc2c139726e8bf27b1de0eb9b088f742`
- Installed app: `/Applications/Ernie.app`
- Installed source: `/Users/thor/.zenbu/apps/ernie`
- Original backup: `/Users/thor/Library/Application Support/Ernie Local Backups/20260909-25458ea`

The backup contains the original app, complete source and dependencies, and app-history storage. Source replacement kept the live `.zenbu` database directory and existing dependencies. No sessions or credentials were replaced.

## Verified behavior

The installed host and editable child run from `/Applications/Ernie.app`. All 237 bundled source files match the installed source. The history controller marked the active checkpoint startup-checked and reports no unsaved changes or capture error.

The renderer serves `DEV=false` and `PROD=true`. Its DialKit and Agentation gates are disabled. The installed history API returns an empty user list; internal launch snapshots no longer appear there. Explicit saves and registered customization intervals remain supported.

A read-only handshake with the existing Prime Agent reports package 0.9.3, protocol 7, and schema revision 26. The original daemon PID 17886 and canonical development Electron PID 43449 remained alive throughout installation. No daemon was installed or restarted.

Validation passed: TypeScript, scoped Ultracite checks, 15 filesystem/transport history integration cases, `lat check`, archive integrity, and strict recursive code-signature verification. The archive uses ad-hoc signing without notarization.

## Remaining verification

Native computer inspection selects the windowless recovery host instead of the editable app window. The previous candidate was visually connected, but the final candidate still needs direct visual confirmation of Settings navigation and all folder-button states. The new labels are `choose folder`, `choose folder?` on hover, and `choosing...` while selecting.

The previous candidate displayed an update-check failure. Remote source publication and an earlier-version update were not verified in this task. Do not describe automatic updates as proven by this local installation. A downloaded archive on another Mac may require macOS approval because it is not notarized.

## Blog notes and screenshots to capture

Build the post around the separation between Ernie’s interface, its editable local source, and the existing Prime Agent daemon. Describe this as an installed local candidate until a release tag includes these fixes.

Useful screenshots are the installed welcome screen without tuning tools, the Connected details, Appearance settings, empty App history before customization, and one completed user customization with a restore review. Capture folder-picker feedback only after testing idle, hover, open, and cancel states.

Avoid screenshots containing session contents, credentials, local connection tokens, or unrelated personal paths. Explain that recovery checkpoints exclude conversations and credentials; do not claim that every feature works because startup passed.

Follow [the manual release instructions](../releasing.md) for the next candidate. Publication requires an explicitly authorized follow-up, a clean committed candidate, current release notes, and a fresh check of remote tags.

## App switcher correction

The rebuilt host uses macOS accessory activation policy. Live process metadata confirmed the recovery parent as accessory (1) and the editable child as regular (0), leaving one normal app-switcher entry. Automatic updates remain disabled by default. The previous app is retained as `Ernie-before-dock-fix.app` in the backup directory.
