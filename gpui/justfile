set shell := ["zsh", "-fcu"]

dev:
    cargo run -p gpui-app-desktop

run:
    cargo run --release -p gpui-app-desktop

build:
    cargo build --release --locked -p gpui-app-desktop

check:
    cargo fmt --all -- --check
    cargo check --workspace --locked --all-targets
    cargo test --workspace --locked
    cargo clippy --workspace --locked --all-targets -- -D warnings
