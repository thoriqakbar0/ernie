import * as stylex from "@stylexjs/stylex"

/** Fills the sidebar injection slot without changing the shell layout. */
export const styles = stylex.create({ sidebarView: { height: "100%", width: "100%" } })
