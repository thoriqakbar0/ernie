import * as stylex from "@stylexjs/stylex"
import { theme } from "../theme.stylex"

const popupIn = stylex.keyframes({
  from: {
    opacity: "0",
    transform: "translateY(4px)",
  },
  to: {
    opacity: "1",
    transform: "translateY(0)",
  },
})
/** Styles owned by this surface, including its responsive and interaction states. */
export const styles = stylex.create({
  effortTrigger: {
    minWidth: "82px",
  },
  hiddenModelsToggle: {
    alignSelf: "flex-start",
    backgroundColor: "transparent",
    borderStyle: "solid",
    borderWidth: "0",
    color: {
      ":focus-visible": theme["--ink"],
      ":hover": theme["--ink"],
      default: theme["--muted"],
    },
    cursor: "pointer",
    fontSize: "10px",
    margin: "7px 10px 0",
  },
  modelEffortControl: {
    alignItems: "center",
    borderTopColor: theme["--rule"],
    borderTopStyle: "solid",
    borderTopWidth: "1px",
    color: theme["--muted"],
    display: "flex",
    flexShrink: "0",
    fontSize: "12px",
    gap: "12px",
    justifyContent: "space-between",
    minHeight: "52px",
    padding: "8px 12px",
  },
  modelEmpty: {
    color: theme["--muted"],
    display: "grid",
    fontSize: "12px",
    gap: "12px",
    justifyItems: "center",
    margin: "0",
    padding: "34px 18px",
    textAlign: "center",
  },
  modelEmptyAction: {
    backgroundColor: { ":hover": theme["--surface"], default: "transparent" },
    borderColor: theme["--rule"],
    borderRadius: "8px",
    borderStyle: "solid",
    borderWidth: "1px",
    color: theme["--ink"],
    minHeight: { "@media (max-width: 720px)": "44px", default: "40px" },
    padding: "0 11px",
  },
  modelName: {
    color: theme["--ink-strong"],
    fontSize: "13px",
    fontWeight: "680",
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  modelOption: {
    alignItems: "center",
    backgroundColor: "transparent",
    borderRadius: "9px",
    borderStyle: "solid",
    borderWidth: "0",
    cursor: "pointer",
    display: "flex",
    gap: "9px",
    minHeight: "44px",
    padding: "5px 8px",
    scale: {
      ":active": "0.96",
      default: null,
    },
    textAlign: "left",
    transition: "background-color 100ms ease-out, color 100ms ease-out, scale 150ms ease-out",
    width: "100%",
  },
  modelOptionCopy: {
    alignItems: "center",
    display: "flex",
    flex: "1",
    gap: "12px",
    justifyContent: "space-between",
    minWidth: "0",
  },
  modelOptionHide: {
    backgroundColor: {
      ":hover": theme["--surface"],
      default: "transparent",
    },
    borderRadius: "8px",
    borderStyle: "solid",
    borderWidth: "0",
    color: {
      ":hover": theme["--accent"],
      default: theme["--muted"],
    },
    cursor: "pointer",
    display: "grid",
    flex: "0 0 auto",
    height: "34px",
    opacity: {
      ":focus-visible": "1",
      [stylex.when.ancestor(":hover")]: 1,
      [stylex.when.ancestor(":focus-within")]: 1,
      "@media (hover: none)": "1",
      default: "0",
    },
    placeItems: "center",
    scale: {
      ":active": "0.96",
      default: null,
    },
    transition:
      "background-color 100ms ease-out, color 100ms ease-out, opacity 100ms ease-out, scale 150ms ease-out",
    width: "30px",
  },
  modelOptionLabel: {
    alignItems: "baseline",
    display: "flex",
    gap: "7px",
    minWidth: "0",
  },
  modelOptionPin: {
    backgroundColor: {
      ":hover": theme["--surface"],
      default: "transparent",
    },
    borderRadius: "8px",
    borderStyle: "solid",
    borderWidth: "0",
    color: {
      ":hover": theme["--accent"],
      ':is([aria-pressed="true"])': theme["--accent"],
      default: theme["--muted"],
    },
    cursor: "pointer",
    display: "grid",
    flex: "0 0 auto",
    height: "34px",
    opacity: {
      ":focus-visible": "1",
      ':is([aria-pressed="true"])': "1",
      [stylex.when.ancestor(":hover")]: 1,
      [stylex.when.ancestor(":focus-within")]: 1,
      "@media (hover: none)": "1",
      default: "0",
    },
    placeItems: "center",
    scale: {
      ":active": "0.96",
      default: null,
    },
    transition:
      "background-color 100ms ease-out, color 100ms ease-out, opacity 100ms ease-out, scale 150ms ease-out",
    width: "30px",
  },
  modelOptionRow: {
    alignItems: "center",
    backgroundColor: {
      ":focus-within": theme["--surface-muted"],
      ':has(> [role="option"][aria-selected="true"])': theme["--surface-muted"],
      ":hover": theme["--surface-muted"],
      default: "transparent",
    },
    borderRadius: "7px",
    display: "flex",
    transition: "background-color 100ms ease-out",
  },
  modelOptionRowHidden: {
    opacity: "0.55",
  },
  modelOptions: {
    minHeight: "0",
    overflowY: "auto",
    padding: "6px",
  },
  modelPicker: {
    minWidth: "0",
    position: "relative",
  },
  modelPopup: {
    animationDuration: "150ms",
    animationIterationCount: "1",
    animationName: popupIn,
    animationTimingFunction: "ease-out",
    backgroundColor: theme["--surface"],
    borderColor: theme["--rule"],
    borderRadius: "12px",
    borderStyle: "solid",
    borderWidth: "1px",
    boxShadow: "0 22px 54px -22px rgb(43 26 7 / 0.48)",
    color: theme["--ink"],
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
    position: "fixed",
    zIndex: "40",
  },
  modelSearch: {
    alignItems: "center",
    backgroundColor: { ":focus-within": theme["--surface-muted"], default: "transparent" },
    borderBottomColor: theme["--rule"],
    borderBottomStyle: "solid",
    borderBottomWidth: "1px",
    color: theme["--muted"],
    display: "flex",
    gap: "9px",
    padding: "10px 12px",
  },
  modelSearchInput: {
    "::placeholder": {
      color: theme["--faint"],
    },
    backgroundColor: "transparent",
    borderStyle: "solid",
    borderWidth: "0",
    color: theme["--ink-strong"],
    flex: "1",
    fontSize: "13px",
    height: "34px",
    minWidth: "0",
    outlineStyle: "none",
  },
  modelTier: {
    color: theme["--muted"],
    flex: "0 0 auto",
    fontSize: "10px",
    fontWeight: "520",
  },
  modelTrigger: {
    alignItems: "center",
    backgroundColor: {
      ":hover:not(:disabled)": theme["--surface-muted"],
      ':is([aria-expanded="true"])': theme["--surface-muted"],
      default: "transparent",
    },
    borderColor: {
      ":hover:not(:disabled)": theme["--rule"],
      ':is([aria-expanded="true"])': theme["--rule"],
      default: "transparent",
    },
    borderRadius: "8px",
    borderStyle: "solid",
    borderWidth: "1px",
    color: {
      ":hover:not(:disabled)": theme["--ink"],
      ':is([aria-expanded="true"])': theme["--ink"],
      default: theme["--muted"],
    },
    cursor: "pointer",
    display: "inline-flex",
    fontSize: "11px",
    gap: "7px",
    maxWidth: {
      "@media (max-width: 480px)": "150px",
      default: "220px",
    },
    minHeight: "34px",
    minWidth: "0",
    opacity: {
      ":disabled": "0.5",
      default: null,
    },
    padding: "6px 10px",
  },
  modelTriggerLabel: {
    minWidth: "0",
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  providerFilter: {
    backgroundColor: {
      ':is([aria-pressed="true"])': theme["--ink"],
      default: theme["--surface-muted"],
    },
    borderColor: {
      ':is([aria-pressed="true"])': theme["--ink"],
      default: theme["--rule"],
    },
    borderRadius: "8px",
    borderStyle: "solid",
    borderWidth: "1px",
    color: {
      ':is([aria-pressed="true"])': theme["--surface"],
      default: theme["--muted"],
    },
    cursor: "pointer",
    display: "grid",
    flex: "0 0 auto",
    fontFamily: "var(--font-mono, ui-monospace, monospace)",
    fontSize: "10px",
    fontWeight: "800",
    height: "34px",
    placeItems: "center",
    textTransform: "uppercase",
    width: "34px",
  },
  providerFilters: {
    alignItems: "center",
    borderBottomColor: theme["--rule"],
    borderBottomStyle: "solid",
    borderBottomWidth: "1px",
    display: "flex",
    gap: "6px",
    minHeight: "50px",
    overflowX: "auto",
    padding: "7px 10px",
  },
  providerName: {
    color: theme["--muted"],
    fontSize: "10px",
    fontWeight: "780",
    letterSpacing: "0.08em",
    margin: "0",
    padding: "8px 8px 5px",
    textTransform: "uppercase",
  },
  rowOption: {
    flex: "1",
    minWidth: "0",
    width: "auto",
  },
  selectedIcon: {
    color: theme["--accent"],
  },
})
