/** Renders the native draggable region at the top of the window. */
export function Titlebar() {
  return (
    <div className="app-titlebar h-[38px] shrink-0 border-b border-zinc-200 dark:border-zinc-800" />
  )
}
